{
  "name": "image-mcp-widget",
  "version": "0.1.0",
  "description": "React widget for displaying Pixabay image search results inside ChatGPT.",
  "license": "MIT",
  "author": "",
  "type": "module",
  "main": "dist/component.js",
  "scripts": {
    "build": "esbuild src/main.tsx --bundle --format=esm --outfile=dist/component.js --sourcemap --define:process.env.NODE_ENV='\"production\"'",
    "typecheck": "tsc --noEmit",
    "dev": "esbuild src/main.tsx --bundle --format=esm --outfile=dist/component.js --sourcemap --watch"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.10",
    "@types/react-dom": "^18.3.4",
    "esbuild": "^0.23.0",
    "typescript": "^5.4.5"
  }
}
